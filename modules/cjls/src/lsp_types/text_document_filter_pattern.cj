/**
 * Generated by lsp_codegen from metaModel.json 3.18.0.
 */
package cjls.lsp_types

import std.collection.*
import std.deriving.*
import stdx.serialization.serialization.*
import stdxx.*
import stdxx.deriving.*
import jsonrpc.*

/**
 * A document filter where `pattern` is required field.
 *
 * @since 3.18.0
 */
@DeriveExt[Serializable]
@DeriveExtSerializable[skipNone]
public struct TextDocumentFilterPattern {
    public TextDocumentFilterPattern(
        /**
         * A language id, like `typescript`.
         */
        public let language!: ?String = None,

        /**
         * A Uri {@link Uri.scheme scheme}, like `file` or `untitled`.
         */
        public let scheme!: ?String = None,

        /**
         * A glob pattern, like **​/\*.{ts,js}. See TextDocumentFilter for examples.
         *
         * @since 3.18.0 - support for relative patterns. Whether clients support
         * relative patterns depends on the client capability
         * `textDocuments.filters.relativePatternSupport`.
         */
        public let pattern!: GlobPattern
    ) {}
}
