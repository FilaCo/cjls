/**
 * Generated by lsp_codegen from metaModel.json 3.18.0.
 */
package cjls.lsp_types

import std.deriving.*
import stdx.serialization.serialization.*
import stdxx.*
import jsonrpc.*

/**
 * The moniker kind.
 *
 * @since 3.16.0
 */
@Derive[Equatable, Hashable]
public enum MonikerKind {
    /**
     * The moniker represent a symbol that is imported into a project
     */
    | Import

    /**
     * The moniker represents a symbol that is exported from a project
     */
    | Export

    /**
     * The moniker represents a symbol that is local to a project (e.g. a local
     * variable of a function, a class not visible outside the project, ...)
     */
    | Local
}

extend MonikerKind <: Serializable<MonikerKind> {
    public static func deserialize(dm: DataModel): MonikerKind {
        match (dm.asStringOrThrow()) {
            case "import" => Import
            case "export" => Export
            case "local" => Local
            case v => throw DataModelException("unexpected variant ${v}")
        }
    }
    public func serialize(): DataModel {
        match (this) {
            case Import => DataModelString("import")
            case Export => DataModelString("export")
            case Local => DataModelString("local")
        }
    }
}
