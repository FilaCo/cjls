/**
 * Generated by lsp_codegen from metaModel.json 3.18.0.
 */
package cjls.lsp_types

import std.collection.*
import std.deriving.*
import stdx.serialization.serialization.*
import stdxx.{IntegerOrString, Nullable}
import stdxx.serialization.*
import stdxx.deriving.*
import jsonrpc.*

/**
 * An inline completion item represents a text snippet that is proposed inline to complete text that is being typed.
 *
 * @since 3.18.0
 */
@DeriveExt[Serializable]
@DeriveExtSerializable[skipNone]
public struct InlineCompletionItem {
    public InlineCompletionItem(
        /**
         * The text to replace the range with. Must be set.
         */
        public let insertText!: StringOrStringValue,

        /**
         * A text that is used to decide if this inline completion should be shown. When `falsy` the {@link InlineCompletionItem.insertText} is used.
         */
        public let filterText!: ?String = None,

        /**
         * The range to replace. Must begin and end on the same line.
         */
        public let range!: ?Range = None,

        /**
         * An optional {@link Command} that is executed *after* inserting this completion.
         */
        public let command!: ?Command = None
    ) {}
}
