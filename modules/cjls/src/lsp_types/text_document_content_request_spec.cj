/**
 * Generated by lsp_codegen from metaModel.json 3.18.0.
 */
package cjls.lsp_types

import std.collection.*
import std.deriving.*
import stdx.serialization.serialization.*
import stdxx.{IntegerOrString, Nullable}
import stdxx.serialization.*
import stdxx.deriving.*
import jsonrpc.*

/**
 * The `workspace/textDocumentContent` request is sent from the client to the
 * server to request the content of a text document.
 *
 * @since 3.18.0
 */
public struct TextDocumentContentRequestSpec <: LspInboundRequest<TextDocumentContentParams, TextDocumentContentResult> {
    public static prop METHOD: String {
        get() {
            "workspace/textDocumentContent"
        }
    }

    public static prop DIRECTION: MessageDirection {
        get() {
            MessageDirection.ClientToServer
        }
    }

    public prop method: String {
        get() {
            METHOD
        }
    }

    public func decodeParams(params: ?ArrayOrObject): TextDocumentContentParams {
        decodeMessageParams<TextDocumentContentParams>(params)
    }

    public func encodeResult(result: TextDocumentContentResult): DataModel {
        result.serialize()
    }
}
