/**
 * Generated by lsp_codegen from metaModel.json 3.18.0.
 */
package cjls.lsp_types

import std.collection.*
import std.deriving.*
import stdx.serialization.serialization.*
import stdxx.*
import stdxx.deriving.*
import jsonrpc.*

@DeriveExt[Serializable]
@DeriveExtSerializable[skipNone]
public struct FoldingRangeClientCapabilities {
    public FoldingRangeClientCapabilities(
        /**
         * Whether implementation supports dynamic registration for folding range
         * providers. If this is set to `true` the client supports the new
         * `FoldingRangeRegistrationOptions` return value for the corresponding
         * server capability as well.
         */
        public let dynamicRegistration!: ?Bool = None,

        /**
         * The maximum number of folding ranges that the client prefers to receive
         * per document. The value serves as a hint, servers are free to follow the
         * limit.
         */
        public let rangeLimit!: ?UInt32 = None,

        /**
         * If set, the client signals that it only supports folding complete lines.
         * If set, client will ignore specified `startCharacter` and `endCharacter`
         * properties in a FoldingRange.
         */
        public let lineFoldingOnly!: ?Bool = None,

        /**
         * Specific options for the folding range kind.
         *
         * @since 3.17.0
         */
        public let foldingRangeKind!: ?ClientFoldingRangeKindOptions = None,

        /**
         * Specific options for the folding range.
         *
         * @since 3.17.0
         */
        public let foldingRange!: ?ClientFoldingRangeOptions = None
    ) {}
}
