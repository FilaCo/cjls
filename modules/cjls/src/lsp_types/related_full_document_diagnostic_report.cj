/**
 * Generated by lsp_codegen from metaModel.json 3.18.0.
 */
package cjls.lsp_types

import std.collection.*
import std.deriving.*
import stdx.serialization.serialization.*
import stdxx.{IntegerOrString, Nullable}
import stdxx.serialization.*
import stdxx.deriving.*
import jsonrpc.*

/**
 * A full diagnostic report with a set of related documents.
 *
 * @since 3.17.0
 */
@DeriveExt[Serializable]
@DeriveExtSerializable[skipNone, tag: "kind", value: "full"]
public struct RelatedFullDocumentDiagnosticReport {
    public RelatedFullDocumentDiagnosticReport(
        /**
         * An optional result id. If provided it will
         * be sent on the next diagnostic request for the
         * same document.
         */
        public let resultId!: ?String = None,

        /**
         * The actual items.
         */
        public let items!: Array<Diagnostic>,

        /**
         * Diagnostics of related documents. This information is useful
         * in programming languages where code in a file A can generate
         * diagnostics in a file B which A depends on. An example of
         * such a language is C/C++ where marco definitions in a file
         * a.cpp and result in errors in a header file b.hpp.
         *
         * @since 3.17.0
         */
        public let relatedDocuments!: ?HashMap<DocumentUri,
            FullDocumentDiagnosticReportOrUnchangedDocumentDiagnosticReport> = None
    ) {}
}
