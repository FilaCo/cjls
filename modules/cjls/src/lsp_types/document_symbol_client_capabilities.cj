/**
 * Generated by lsp_codegen from metaModel.json 3.18.0.
 */
package cjls.lsp_types

import std.collection.*
import std.deriving.*
import stdx.serialization.serialization.*
import stdxx.*
import stdxx.deriving.*
import jsonrpc.*

/**
 * Client Capabilities for a {@link DocumentSymbolRequest}.
 */
@DeriveExt[Serializable]
@DeriveExtSerializable[skipNone]
public struct DocumentSymbolClientCapabilities {
    public DocumentSymbolClientCapabilities(
        /**
         * Whether document symbol supports dynamic registration.
         */
        public let dynamicRegistration!: ?Bool = None,

        /**
         * Specific capabilities for the `SymbolKind` in the
         * `textDocument/documentSymbol` request.
         */
        public let symbolKind!: ?ClientSymbolKindOptions = None,

        /**
         * The client supports hierarchical document symbols.
         */
        public let hierarchicalDocumentSymbolSupport!: ?Bool = None,

        /**
         * The client supports tags on `SymbolInformation`. Tags are supported on
         * `DocumentSymbol` if `hierarchicalDocumentSymbolSupport` is set to true.
         * Clients supporting tags have to handle unknown tags gracefully.
         *
         * @since 3.16.0
         */
        public let tagSupport!: ?ClientSymbolTagOptions = None,

        /**
         * The client supports an additional label presented in the UI when
         * registering a document symbol provider.
         *
         * @since 3.16.0
         */
        public let labelSupport!: ?Bool = None
    ) {}
}
