/**
 * Generated by lsp_codegen from metaModel.json 3.18.0.
 */
package cjls.lsp_types

import std.collection.*
import std.deriving.*
import stdx.serialization.serialization.*
import stdxx.*
import stdxx.deriving.*
import jsonrpc.*

/**
 * Completion client capabilities
 */
@DeriveExt[Serializable]
@DeriveExtSerializable[skipNone]
public struct CompletionClientCapabilities {
    public CompletionClientCapabilities(
        /**
         * Whether completion supports dynamic registration.
         */
        public let dynamicRegistration!: ?Bool = None,

        /**
         * The client supports the following `CompletionItem` specific
         * capabilities.
         */
        public let completionItem!: ?ClientCompletionItemOptions = None,

        /**
         * The client supports the following completion item kinds.
         */
        public let completionItemKind!: ?ClientCompletionItemOptionsKind = None,

        /**
         * Defines how the client handles whitespace and indentation
         * when accepting a completion item that uses multi line
         * text in either `insertText` or `textEdit`.
         *
         * @since 3.17.0
         */
        public let insertTextMode!: ?InsertTextMode = None,

        /**
         * The client supports to send additional context information for a
         * `textDocument/completion` request.
         */
        public let contextSupport!: ?Bool = None,

        /**
         * The client supports the following `CompletionList` specific
         * capabilities.
         *
         * @since 3.17.0
         */
        public let completionList!: ?CompletionListCapabilities = None
    ) {}
}
