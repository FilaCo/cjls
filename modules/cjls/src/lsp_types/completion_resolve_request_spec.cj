/**
 * Generated by lsp_codegen from metaModel.json 3.18.0.
 */
package cjls.lsp_types

import std.collection.*
import std.deriving.*
import stdx.serialization.serialization.*
import stdxx.{IntegerOrString, Nullable}
import stdxx.serialization.*
import stdxx.deriving.*
import jsonrpc.*

/**
 * Request to resolve additional information for a given completion item.The request's
 * parameter is of type {@link CompletionItem} the response
 * is of type {@link CompletionItem} or a Thenable that resolves to such.
 */
public struct CompletionResolveRequestSpec <: LspInboundRequest<CompletionItem, CompletionItem> {
    public static prop METHOD: String {
        get() {
            "completionItem/resolve"
        }
    }

    public static prop DIRECTION: MessageDirection {
        get() {
            MessageDirection.ClientToServer
        }
    }

    public prop method: String {
        get() {
            METHOD
        }
    }

    public func decodeParams(params: ?ArrayOrObject): CompletionItem {
        decodeMessageParams<CompletionItem>(params)
    }

    public func encodeResult(result: CompletionItem): DataModel {
        result.serialize()
    }
}
