/**
 * Generated by lsp_codegen from metaModel.json 3.18.0.
 */
package cjls.lsp_types

import std.collection.*
import std.deriving.*
import stdx.serialization.serialization.*
import stdxx.*
import stdxx.deriving.*
import jsonrpc.*

/**
 * Client capabilities for a {@link WorkspaceSymbolRequest}.
 */
@DeriveExt[Serializable]
@DeriveExtSerializable[skipNone]
public struct WorkspaceSymbolClientCapabilities {
    public WorkspaceSymbolClientCapabilities(
        /**
         * Symbol request supports dynamic registration.
         */
        public let dynamicRegistration!: ?Bool = None,

        /**
         * Specific capabilities for the `SymbolKind` in the `workspace/symbol` request.
         */
        public let symbolKind!: ?ClientSymbolKindOptions = None,

        /**
         * The client supports tags on `SymbolInformation`.
         * Clients supporting tags have to handle unknown tags gracefully.
         *
         * @since 3.16.0
         */
        public let tagSupport!: ?ClientSymbolTagOptions = None,

        /**
         * The client support partial workspace symbols. The client will send the
         * request `workspaceSymbol/resolve` to the server to resolve additional
         * properties.
         *
         * @since 3.17.0
         */
        public let resolveSupport!: ?ClientSymbolResolveOptions = None
    ) {}
}
