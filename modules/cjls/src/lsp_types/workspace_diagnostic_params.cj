/**
 * Generated by lsp_codegen from metaModel.json 3.18.0.
 */
package cjls.lsp_types

import std.collection.*
import std.deriving.*
import stdx.serialization.serialization.*
import stdxx.*
import stdxx.deriving.*
import jsonrpc.*

/**
 * Parameters of the workspace diagnostic request.
 *
 * @since 3.17.0
 */
@DeriveExt[Serializable]
@DeriveExtSerializable[skipNone]
public struct WorkspaceDiagnosticParams {
    public WorkspaceDiagnosticParams(
        /**
         * An optional token that a server can use to report work done progress.
         */
        public let workDoneToken!: ?ProgressToken = None,

        /**
         * An optional token that a server can use to report partial results (e.g. streaming) to
         * the client.
         */
        public let partialResultToken!: ?ProgressToken = None,

        /**
         * The additional identifier provided during registration.
         */
        public let identifier!: ?String = None,

        /**
         * The currently known diagnostic reports with their
         * previous result ids.
         */
        public let previousResultIds!: Array<PreviousResultId>
    ) {}
}
