/**
 * Generated by lsp_codegen from metaModel.json 3.18.0.
 */
package cjls.lsp_types

import std.collection.*
import std.deriving.*
import stdx.serialization.serialization.*
import stdxx.*
import stdxx.deriving.*
import jsonrpc.*

/**
 * The Client Capabilities of a {@link CodeActionRequest}.
 */
@DeriveExt[Serializable]
@DeriveExtSerializable[skipNone]
public struct CodeActionClientCapabilities {
    public CodeActionClientCapabilities(
        /**
         * Whether code action supports dynamic registration.
         */
        public let dynamicRegistration!: ?Bool = None,

        /**
         * The client support code action literals of type `CodeAction` as a valid
         * response of the `textDocument/codeAction` request. If the property is not
         * set the request can only return `Command` literals.
         *
         * @since 3.8.0
         */
        public let codeActionLiteralSupport!: ?ClientCodeActionLiteralOptions = None,

        /**
         * Whether code action supports the `isPreferred` property.
         *
         * @since 3.15.0
         */
        public let isPreferredSupport!: ?Bool = None,

        /**
         * Whether code action supports the `disabled` property.
         *
         * @since 3.16.0
         */
        public let disabledSupport!: ?Bool = None,

        /**
         * Whether code action supports the `data` property which is
         * preserved between a `textDocument/codeAction` and a
         * `codeAction/resolve` request.
         *
         * @since 3.16.0
         */
        public let dataSupport!: ?Bool = None,

        /**
         * Whether the client supports resolving additional code action
         * properties via a separate `codeAction/resolve` request.
         *
         * @since 3.16.0
         */
        public let resolveSupport!: ?ClientCodeActionResolveOptions = None,

        /**
         * Whether the client honors the change annotations in
         * text edits and resource operations returned via the
         * `CodeAction#edit` property by for example presenting
         * the workspace edit in the user interface and asking
         * for confirmation.
         *
         * @since 3.16.0
         */
        public let honorsChangeAnnotations!: ?Bool = None,

        /**
         * Whether the client supports documentation for a class of
         * code actions.
         *
         * @since 3.18.0
         */
        public let documentationSupport!: ?Bool = None,

        /**
         * Client supports the tag property on a code action. Clients
         * supporting tags have to handle unknown tags gracefully.
         *
         * @since 3.18.0
         */
        public let tagSupport!: ?CodeActionTagOptions = None
    ) {}
}
