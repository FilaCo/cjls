/**
 * Generated by lsp_codegen from metaModel.json 3.18.0.
 */
package cjls.lsp_types

import std.collection.*
import std.deriving.*
import stdx.serialization.serialization.*
import stdxx.*
import stdxx.deriving.*
import jsonrpc.*

/**
 * Capabilities relating to events from file operations by the user in the client.
 *
 * These events do not come from the file system, they come from user operations
 * like renaming a file in the UI.
 *
 * @since 3.16.0
 */
@DeriveExt[Serializable]
@DeriveExtSerializable[skipNone]
public struct FileOperationClientCapabilities {
    public FileOperationClientCapabilities(
        /**
         * Whether the client supports dynamic registration for file requests/notifications.
         */
        public let dynamicRegistration!: ?Bool = None,

        /**
         * The client has support for sending didCreateFiles notifications.
         */
        public let didCreate!: ?Bool = None,

        /**
         * The client has support for sending willCreateFiles requests.
         */
        public let willCreate!: ?Bool = None,

        /**
         * The client has support for sending didRenameFiles notifications.
         */
        public let didRename!: ?Bool = None,

        /**
         * The client has support for sending willRenameFiles requests.
         */
        public let willRename!: ?Bool = None,

        /**
         * The client has support for sending didDeleteFiles notifications.
         */
        public let didDelete!: ?Bool = None,

        /**
         * The client has support for sending willDeleteFiles requests.
         */
        public let willDelete!: ?Bool = None
    ) {}
}
