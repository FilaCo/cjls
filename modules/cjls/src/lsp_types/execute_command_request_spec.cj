/**
 * Generated by lsp_codegen from metaModel.json 3.18.0.
 */
package cjls.lsp_types

import std.collection.*
import std.deriving.*
import stdx.serialization.serialization.*
import stdxx.{IntegerOrString, Nullable}
import stdxx.serialization.*
import stdxx.deriving.*
import jsonrpc.*

/**
 * A request send from the client to the server to execute a command. The request might return
 * a workspace edit which the client will apply to the workspace.
 */
public struct ExecuteCommandRequestSpec <: LspInboundRequest<ExecuteCommandParams, LSPAny> {
    public static prop METHOD: String {
        get() {
            "workspace/executeCommand"
        }
    }

    public static prop DIRECTION: MessageDirection {
        get() {
            MessageDirection.ClientToServer
        }
    }

    public init() {}

    public prop method: String {
        get() {
            METHOD
        }
    }

    public func decodeParams(params: ?ArrayOrObject): ExecuteCommandParams {
        decodeMessageParams<ExecuteCommandParams>(params)
    }

    public func encodeResult(result: LSPAny): DataModel {
        result.serialize()
    }
}
