/**
 * Generated by lsp_codegen from metaModel.json 3.18.0.
 */
package cjls.lsp_types

import std.collection.*
import std.deriving.*
import stdx.serialization.serialization.*
import stdxx.{IntegerOrString, Nullable}
import stdxx.serialization.*
import stdxx.deriving.*
import jsonrpc.*

/**
 * A notebook cell.
 *
 * A cell's document URI must be unique across ALL notebook
 * cells and can therefore be used to uniquely identify a
 * notebook cell or the cell's text document.
 *
 * @since 3.17.0
 */
@DeriveExt[Serializable]
@DeriveExtSerializable[skipNone]
public struct NotebookCell {
    public NotebookCell(
        /**
         * The cell's kind
         */
        public let kind!: NotebookCellKind,

        /**
         * The URI of the cell's text document
         * content.
         */
        public let document!: DocumentUri,

        /**
         * Additional metadata stored with the cell.
         *
         * Note: should always be an object literal (e.g. LSPObject)
         */
        public let metadata!: ?LSPObject = None,

        /**
         * Additional execution summary information
         * if supported by the client.
         */
        public let executionSummary!: ?ExecutionSummary = None
    ) {}
}
